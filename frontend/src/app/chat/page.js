import ChatInterfaceScreen from "@/components/features/ChatInterfaceScreen";

// Renders the chat interface page where a user can interact with Feyn.
export default function ChatPage() {
  return <ChatInterfaceScreen />;
}